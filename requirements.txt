# PAS MVP Dependencies
# Python 3.11+

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Config
pydantic-settings==2.6.1

# Twilio
twilio==9.3.7

# Deepgram (WebSocket streaming)
websockets==13.1

# ElevenLabs / Audio
httpx==0.27.0
pydub==0.25.1
# Note: pydub requires ffmpeg installed on the system
# On Railway: add ffmpeg to nixpacks.toml

# Anthropic
anthropic==0.40.0

# OpenAI (alternative LLM provider — selected via LLM_PROVIDER=openai)
openai==1.54.4

# Supabase
supabase==2.10.0

# Auth — Supabase Auth JWT verification (HS256 access tokens, PAS211G.2).
# Already present transitively (gotrue/twilio) and pinned in constraints.txt;
# declared directly here because PAS now imports it (app/auth/resolver.py).
PyJWT==2.13.0

# Utilities
python-multipart==0.0.12  # for FastAPI form parsing (Twilio webhooks)
