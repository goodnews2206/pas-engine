# PAS Web — environment variable reference
# Copy this file to .env.local for local overrides.
# Never commit .env.local

# Railway backend URL — leave empty until API wiring is connected (Step 11+)
NEXT_PUBLIC_PAS_API_BASE_URL=
