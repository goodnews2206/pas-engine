# sharp is Next.js's optional image optimiser. We don't need it for the
# foundation — Next.js falls back to its built-in squoosh processor.
# pnpm-workspace.yaml has allowBuilds.sharp=false (pnpm 11 approval config).
