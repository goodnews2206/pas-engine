.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.DS_Store

# Runtime logs — NEVER commit. Caused PAS125 leak of brokerage api_keys
# via httpx INFO request-URL logging. Files stay on disk locally.
server_err.log
server_out.log
*.log
server*.log

# Local report / demo artifacts — keep on disk, do not commit.
reports/
backups/
integrity_check_report.json
pas169_launch_readiness_report.json
security_audit_report.json
# PAS211I: readiness-gate scripts emit a JSON report when run — never commit it.
*_readiness_report.json
