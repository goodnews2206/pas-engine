"""
PAS140A — Unified event contract package.

Currently contains only the static contract module (`contract.py`).
Subsequent PAS140 phases will add helpers that consume the contract
(taxonomy validators, replay loaders, event-stream filters).
"""
